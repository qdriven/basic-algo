package io.hedwig.notes.algorithm.sortings;

/**
 * 1. author: patrick
 * 47.	定新数组大小——找出待排序的数组中最大和最小的元素
 48.	统计次数——统计数组中每个值为i的元素出现的次数，存入新数组C的第i项
 49.	对统计次数逐个累加——对所有的计数累加（从C中的第一个元素开始，每一项和前一项相加）
 50.	反向填充目标数组——将每个元素i放在新数组的第C(i)项，每放一个元素就将C(i)减去1

 */
public class CountingSorter {

}
